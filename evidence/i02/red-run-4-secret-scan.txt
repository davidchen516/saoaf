# I02 红运行④：密钥泄露 → secret scan 门禁拦截（本地 + CI 双证据）

## CI 证据（权威）

- 探针 PR #27（draft，已关闭）：分支注入随机生成的 AWS 格式垃圾密钥（非真实凭证）
- quality workflow SCA job → gitleaks-action 8.24.3 检出：
  - RuleID: `aws-access-token`，File: `creds-probe.txt`
  - Fingerprint: 4cfdc24…:creds-probe.txt:aws-access-token
  - exit code 2 → SCA job 失败 → PR mergeStateStatus BLOCKED（必检阻断合并）
- Run: https://github.com/davidchen516/saoaf/actions/runs/35706492628
- 探针分支已删除；密钥为随机垃圾值，从未存在于任何长存分支

## 本地复证（gitleaks 8.30.1，同一规则集）

$ gitleaks detect --source . --redact
WRN leaks found: 1

## 备注

gitleaks 默认 allowlist 排除文档示例键（含 "EXAMPLE" 字样），探针因此使用随机格式键；
首个探针分支因动作 SHA 无效 + 示例键不可检出而重做。
